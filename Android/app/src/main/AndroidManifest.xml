<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uniting.android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.android.vending.BILLING" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">


        <!--meta-data-->
        <meta-data
        android:name="com.naver.maps.map.CLIENT_ID"
        android:value="4fk2n2jfrm" />
        <!--meta-data-->

        <!--activity-->
        <activity android:name=".Cafeteria.RecyclerViewActivity"></activity>
        <activity android:name=".Cafeteria.CafeteriaInformActivity" />
        <activity android:name=".Room.MakeRoomActivity"
            android:theme="@style/AppTheme.StatusBar"></activity>
        <activity android:name=".Cafeteria.WriteReviewActivity"></activity>
        <activity android:name=".Room.RoomListActivity" />
        <activity android:name=".Chat.ChatActivity" />
        <activity android:name=".Login.LoginActivity" />
        <activity android:name=".Main.SplashActivity" />
        <activity android:name=".Main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--activity-->

        <!--service-->

        <!--service-->
    </application>

</manifest>